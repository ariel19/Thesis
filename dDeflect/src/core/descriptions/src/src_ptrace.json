{
	"name" : "Ptrace check",
  "path": "data/detection/source/ptrace.code",
	"system" : "linux",
	"headers" : [
		"<sys/ptrace.h>"
	],
  "description": "Technique tries to trace itself, if it's not possible, then process is already debugged"
}
